[tox]
env_list = py310, py311, py312, py313, py314, py315
isolated_build = true

requires =
    tox-uv>=1.28

[testenv]
runner = uv-venv-lock-runner
dependency_groups = dev
commands =
    pytest -q -m "not integration" --timeout=120
